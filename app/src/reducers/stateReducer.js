const initialState = {
  loading: false,
  data: {},
  error: "",
};
export const stateReducer = (state = initialState, action) => {
  switch ((action, type)) {
    default:
      return state;
  }
};
